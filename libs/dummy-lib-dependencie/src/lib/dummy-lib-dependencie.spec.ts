import { dummyLibDependencie } from './dummy-lib-dependencie';

describe('dummyLibDependencie', () => {
  it('should work', () => {
    expect(dummyLibDependencie()).toEqual('dummy-lib-dependencie');
  });
});
